# To DO:
# Reinstate RESUME and START toolhead management

[gcode_macro _CLIENT_VARIABLE]
variable_use_custom_pos   : True  ; use custom park coordinates for x,y [True/False]
variable_custom_park_x    : 170   ; custom x position; value must be within your defined min and max of X
variable_custom_park_y    : 90   ; custom y position; value must be within your defined min and max of Y
variable_custom_park_dz   : 15.0   ; custom dz value; the value in mm to lift the nozzle when move to park position
variable_retract          : 2.25   ; the value to retract while PAUSE
variable_cancel_retract   : 2.25   ; the value to retract while CANCEL_PRINT
variable_speed_retract    : 40.0  ; retract speed in mm/s
variable_unretract        : 4.25   ; the value to unretract while RESUME
variable_speed_unretract  : 40.0  ; unretract speed in mm/s
variable_speed_hop        : 15.0  ; z move speed in mm/s
variable_speed_move       : 100.0 ; move speed in mm/s
variable_park_at_cancel   : True ; allow to move the toolhead to park while execute CANCEL_PRINT [True/False]
variable_park_at_cancel_x : 170  ; different park position during CANCEL_PRINT [None/Position as Float]; park_at_cancel must be True
variable_park_at_cancel_y : 90  ; different park position during CANCEL_PRINT [None/Position as Float]; park_at_cancel must be True
# !!! Caution [firmware_retraction] must be defined in the printer.cfg if you set use_fw_retract: True !!!
variable_use_fw_retract   : False ; use fw_retraction instead of the manual version [True/False]
variable_idle_timeout     : 35000     ; time in sec until idle_timeout kicks in. Value 0 means that no value will be set or restored
variable_runout_sensor    : ""    ; If a sensor is defined, it will be used to cancel the execution of RESUME in case no filament is detected.
#                                   Specify the config name of the runout sensor e.g "filament_switch_sensor runout". Hint use the same as in your printer.cfg
# !!! Custom macros, please use with care and review the section of the corresponding macro.
# These macros are for simple operations like setting a status LED. Please make sure your macro does not interfere with the basic macro functions.
# Only  single line commands are supported, please create a macro if you need more than one command.
variable_user_pause_macro : "AFTER_PAUSE"    ; Everything inside the "" will be executed after the klipper base pause (PAUSE_BASE) function
variable_user_resume_macro: "BEFORE_RESUME"    ; Everything inside the "" will be executed before the klipper base resume (RESUME_BASE) function
variable_user_cancel_macro: "BEFORE_CANCEL"    ; Everything inside the "" will be executed before the klipper base cancel (CANCEL_PRINT_BASE) function
gcode:

[gcode_macro qgl]
gcode:
  QUAD_GANTRY_LEVEL
  
# Slicer Start GCODE:
# SET_PRINT_STATS_INFO TOTAL_LAYER=[total_layer_count]
# MODE_SET IST0={is_extruder_used[0]} IST1={is_extruder_used[1]}
# PRINT_WARMUP BED_TEMP={first_layer_bed_temperature[0]} IST0={is_extruder_used[0]} IST1={is_extruder_used[1]}
# PRINT_START T0_TEMP={first_layer_temperature[0]} T1_TEMP={first_layer_temperature[1]} BED_TEMP={first_layer_bed_temperature[0]} TOOL_TEMP={first_layer_temperature[initial_tool]} TOOL={initial_tool} IST0={is_extruder_used[0]} IST1={is_extruder_used[1]}

[gcode_macro MODE_SET]
variable_mode: -1
gcode:
  {% if params.IST0|lower == 'true' and params.IST1|lower == 'true' %}
    SET_GCODE_VARIABLE MACRO=MODE_SET VARIABLE=mode VALUE=-1
    RESPOND MSG="Mode: -1" ; All: -1, T0: 0, T1: 1, Error: 2}
  {% elif params.IST0|lower == 'true' and params.IST1|lower == 'false' %}
    SET_GCODE_VARIABLE MACRO=MODE_SET VARIABLE=mode VALUE=0
    RESPOND MSG="Mode: 0" ; All: -1, T0: 0, T1: 1, Error: 2}
  {% elif params.IST1|lower == 'true' and params.IST0|lower == 'false' %}
    SET_GCODE_VARIABLE MACRO=MODE_SET VARIABLE=mode VALUE=1
    RESPOND MSG="Mode: 1" ; All: -1, T0: 0, T1: 1, Error: 2}
  {% endif %}
  
[gcode_macro PRINT_WARMUP]
description: Perform initial homing and heating tasks
gcode:
    {% set t0_used = params.IST0|upper %}
    {% set t1_used = params.IST1|upper %}
    RESPOND MSG="PRINT_WARMUP!"
    
    {% set BED_TEMP = params.BED_TEMP | int %}
    STOP_FILAMENT_SENSORS ;confirm filament sensors disabled

    RESPOND MSG="Homing"
    G28                             ; home all axes
    G0 Z25
    BED_HEATSOAK TEMP={BED_TEMP}
      

[gcode_macro PRINT_START]

gcode:
  RESPOND MSG="PRINT_START!"
  CRASH_DETECTION_DEFAULTS
    
  INITIALIZE_TOOLCHANGER    
  
  {% set TOOL = params.TOOL | default(0)| int %}
  {% set TOOL_SELECTED = printer[printer.toolchanger.tool] %}
  {% set TOOL_TEMP = params.TOOL_TEMP | default(0) | int %}
  {% set BED_TEMP = params.BED_TEMP | default(0) | int %}
  {% set T0_TEMP = params.T0_TEMP | default(0)| int %}
  {% set T1_TEMP = params.T1_TEMP | default(0)| int %}
  {% set t0_used = params.IST0|upper %}
  {% set t1_used = params.IST1|upper %}
  {% set MODE_START = printer["gcode_macro MODE_SET"].mode %}
  
  STOP_FILAMENT_SENSORS ;confirm filament sensors disabled and crash is off
  STOP_CRASH_DETECTION
  
  {% if not 'xyz' in printer.toolhead.homed_axes %}
    RESPOND MSG="Homing"
    G28                             ; home all axes if not homed
  {% endif %}
  
  RESPOND MSG="Confirming bed temperature setting of {BED_TEMP} degrees"
  
  M140 S{BED_TEMP}
  
  QUAD_GANTRY_LEVEL
   
  {% if MODE_START == -1 %}; If both toolheads are used, heat them both to probing temp, select T0
    T0 TOGGLE_SENSORS=0;
    RESPOND MSG="Heating T0/T1 to probing temp"
    M104 T0 S170
    M109 T1 S170
    RESPOND MSG="Touching!"
    CARTOGRAPHER_TOUCH_HOME
    CALIBRATE_ALL_OFFSETS
    BED_MESH_CALIBRATE ADAPTIVE=1 ; Run adaptive bed mesh
    
    {% if TOOL > 0 %}     ; Switch to the initial printing tool and preheat it.
      M104 T0 S0 ; Shutdown T0.
      T{TOOL} TOGGLE_SENSORS=0; Switch to the initial printing tool
    {% endif %}

    RESPOND MSG="Waiting on T{TOOL}, target={TOOL_TEMP}C"
    M109 T{TOOL} S{TOOL_TEMP} ; Wait for the tool to reach the target temperature
    START_FILAMENT_SENSORS_DELAYED_T{TOOL}
    START_CRASH_DETECTION T={TOOL}

  {% elif MODE_START == 0 %};  If only T0 is used, heat it to probing temp
    INITIALIZE_TOOLCHANGER TOOL_NAME=T0
    RESPOND MSG="Heating T0 to probing temp!"
    M109 T0 S220
    M109 T0 S170
    RESPOND MSG="Touching!"
    CARTOGRAPHER_TOUCH_HOME
    BED_MESH_CALIBRATE ADAPTIVE=1
    RESPOND MSG="Waiting on T0, target={T0_TEMP}C"
    M109 T0 S{T0_TEMP} ; Wait for the tool to reach the target temperature
    RESPOND MSG="RESPOND MSG="Purging!"
    LINE_PURGE
    M109 T0 S{T0_TEMP}
    M400
    START_CRASH_DETECTION T=0
    START_FILAMENT_SENSORS_DELAYED_T0
    RESPOND MSG="Printing!
    
  {% elif MODE_START == 1 %};  If only T1 is used, heat it to probing temp
    INITIALIZE_TOOLCHANGER TOOL_NAME=T1
    RESPOND MSG="Heating T1 to probing temp!"
    M109 T1 S220
    M109 T1 S170
    RESPOND MSG="Touching!"
    CARTOGRAPHER_TOUCH_HOME
    BED_MESH_CALIBRATE ADAPTIVE=1
    RESPOND MSG="Waiting on T1, target={T1_TEMP}C"
    M109 T1 S{T1_TEMP} ; Wait for the tool to reach the target temperature
    RESPOND MSG="Purging!"
    LINE_PURGE
    M109 T1 S{T1_TEMP}
    M400
    START_CRASH_DETECTION T=1
    START_FILAMENT_SENSORS_DELAYED_T1
    RESPOND MSG="Printing!"

    
  {% else %}
    PAUSE
    RESPOND MSG="No toolhead defined!"
  {% endif %}
  



[homing_override]
gcode:
  {% if not 'Z' in printer.toolhead.homed_axes %}
    G91
    SET_KINEMATIC_POSITION SET_HOMED=Z
    G1 Z15
    {% endif %}
  {% if not 'Z' in params and not 'X' in params and 'Y' in params %}
    G28 Y
  {% elif not 'Z' in params and not 'Y' in params and 'X' in params %}
    G28 X
  {% elif not 'Z' in params and 'X' in params and 'Y' in params %}
    G28 Y
    G28 X
  {% elif 'Z' in params and not 'X' in params and not 'Y' in params %}
    G90
    G1 X170 Y175 F4800
    G28 Z
  {% else %}
    G28 Y
    G28 X
    G90
    G1 X170 Y175 F4800
    G28 Z
  {% endif %}
  G90
axes: xyz


[gcode_macro BEFORE_CANCEL]
gcode:

[gcode_macro AFTER_PAUSE]
gcode:
    STOP_FILAMENT_SENSORS
    STOP_CRASH_DETECTION

[gcode_macro BEFORE_RESUME]
gcode:
    UPDATE_DELAYED_GCODE ID=_BED_HEATSOAK_LOOPER DURATION=0
    
    {% if printer["gcode_macro MODE_SET"].mode == -1 %}
      INITIALIZE_TOOLCHANGER
      START_FILAMENT_SENSORS_DELAYED_T{tool}
      START_CRASH_DETECTION T={tool}
    {% elif printer["gcode_macro MODE_SET"].mode == 0 %}
      START_FILAMENT_SENSORS_DELAYED_T0
      START_CRASH_DETECTION T=0
    {% elif MODE_RESUME == 1 %}
      START_FILAMENT_SENSORS_DELAYED_T1
      START_CRASH_DETECTION T=1
    {% endif %}
    START_CRASH_DETECTION

[gcode_macro PRINT_END]
gcode:
    STOP_FILAMENT_SENSORS
    STOP_CRASH_DETECTION
    # Move nozzle away from print while retracting
    G91
    G1 X-2 Y-2 E-2.25 F300
    # Turn off bed, extruder, and fan
    M140 S0
    M104 T0 S0
    M104 T1 S0
    # Raise nozzle by 10mm
    G1 Z10 F3000
    G90
    G1 X170 Y90
    # Disable steppers
    M84
    RESPOND MSG="Print Complete"
  
[gcode_macro STOP_FILAMENT_SENSORS]
gcode:
  SET_FILAMENT_SENSOR SENSOR="switch_sensor_T0" ENABLE=0
  SET_FILAMENT_SENSOR SENSOR="encoder_sensor_T0" ENABLE=0
  SET_FILAMENT_SENSOR SENSOR="switch_sensor_T1" ENABLE=0
  SET_FILAMENT_SENSOR SENSOR="encoder_sensor_T1" ENABLE=0
  # RESPOND TYPE=echo MSG="Filament sensors stopped"

[gcode_macro DOCK_SEQUENCE]
gcode:
  {% for i in range(0, params.REPS|default(10)|int)  %}
    T0
    G4 P1000
    T1
    G4 P1000
  {% endfor %}

[gcode_macro update_git]
gcode:
    {% set message = params.MESSAGE|default() %}
    {% if message %}
        RUN_SHELL_COMMAND CMD=update_git_script_message PARAMS="'{params.MESSAGE}'"
    {% else %}
        RUN_SHELL_COMMAND CMD=update_git_script
    {% endif %}
    
[gcode_macro fix_git]
gcode:
    {% set message = params.MESSAGE|default() %}
    {% if message %}
        RUN_SHELL_COMMAND CMD=update_git_script_message PARAMS="'{params.MESSAGE}'"
    {% else %}
        RUN_SHELL_COMMAND CMD=fix_git_script
    {% endif %}

[gcode_shell_command update_git_script]
command: bash -c "bash $HOME/klipper-backup/script.sh"
timeout: 90.0
verbose: True

[gcode_shell_command fix_git_script]
command: bash -c "bash $HOME/klipper-backup/script.sh --fix"
timeout: 90.0
verbose: True

[gcode_shell_command update_git_script_message]
command: bash -c "bash $HOME/klipper-backup/script.sh -c \"$0\""
timeout: 90.0
verbose: True

[gcode_macro DUMP_VARIABLES]
gcode:
    {% set filter_name = params.NAME|default('')|string|lower %}
    {% set filter_value = params.VALUE|default('')|string|lower %}
    {% set show_cfg = params.SHOW_CFG|default(0)|int %}
    
    {% set out = [] %}

    {% for key1 in printer %}
        {% for key2 in printer[key1] %}
            {% if (show_cfg or not (key1|lower == 'configfile' and key2|lower in ['config', 'settings'])) and (filter_name in key1|lower or filter_name in key2|lower) and filter_value in printer[key1][key2]|string|lower %}
                {% set dummy = out.append("printer['%s'].%s = %s" % (key1, key2, printer[key1][key2])) %}
            {% endif %}
        {% else %}
            {% if filter_name in key1|lower and filter_value in printer[key1]|string|lower %}
                {% set dummy = out.append("printer['%s'] = %s" % (key1, printer[key1])) %}
            {% endif %}
        {% endfor %}
    {% endfor %}
    
    {action_respond_info(out|join("\n"))}